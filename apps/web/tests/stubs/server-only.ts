// `server-only` throws on import outside a React Server Component build.
// Tests import server modules directly, so it resolves to nothing here. The
// real package still guards the application build.
export {};
